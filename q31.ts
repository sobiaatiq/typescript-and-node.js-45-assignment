let users : string [] = ["admin", "eric", "hussain", "Ali", "asad"]
//if(users.length  === 0) {
    //console.log("we need to find some users!")
//}else {
    //for (let user of users){
       // if(user === "admin") {
           // console.log("Hello admin, would you like to use a status repoter?")
        //} else {
            //console.log(`Hello ${user}, thank you for looging in again`)
        //}
    //}
//}
users = []
if (users.length === 0){
    console.log("we need to find some users")
}